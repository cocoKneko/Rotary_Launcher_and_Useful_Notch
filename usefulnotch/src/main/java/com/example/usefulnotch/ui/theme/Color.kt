package com.example.usefulnotch.ui.theme

import androidx.compose.ui.graphics.Color

// Light theme — extracted from reference screenshots
val LightBackground = Color(0xFFF0EEE8)
val LightSurface = Color(0xFFF5F3EE)
val LightOnBackground = Color(0xFF1A1A1A)
val LightOnSurfaceMuted = Color(0xFF8A8780) // labels like "MENU", secondary text
val LightOutline = Color(0xFFD8D5CC) // hairline dividers
val LightAccent = Color(0xFFE5533D) // the red-orange, used sparingly

// Dark theme — designed to match the same character, not a mechanical invert
val DarkBackground = Color(0xFF191816)
val DarkSurface = Color(0xFF232220)
val DarkOnBackground = Color(0xFFECE9E2)
val DarkOnSurfaceMuted = Color(0xFF9C988F)
val DarkOutline = Color(0xFF3A3835)
val DarkAccent = Color(0xFFEF6A52) // slightly brighter than light-mode accent, for dark-bg legibility